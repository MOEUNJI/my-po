// document.addEventListener("mousemove", parallax);
//         function parallax(e) {
//             this.querySelectorAll('.hashtag').forEach(hashtag => {
//                 const speed = hashtag.getAttribute('data-speed');

//                 const x = (window.innerWidth - e.pageX * speed) / 300;
//                 const y = (window.innerHeight - e.pageY * speed) / 30;

//                 hashtag.style.transform = `translateX(${x}px) translateY(${y}px)`;
//             })
//         }